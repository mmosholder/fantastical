{
    "private": true,
    "scripts": {
        "development": "mix",
        "watch": "mix watch",
        "watch-poll": "mix watch -- --watch-options-poll=1000",
        "hot": "mix watch --hot",
        "production": "mix --production",
        "postinstall": "mix --production"
    },
    "dependencies": {
        "laravel-mix": "^6.0.39",
        "splitting": "^1.0.6"
    },
    "devDependencies": {
        "postcss-custom-properties": "^12.0.0",
        "postcss-preset-env": "^7.0.1",
        "resolve-url-loader": "^4.0.0",
        "sass": "^1.44.0",
        "sass-loader": "^12.3.0"
    }
}
